# The public npm registry is the only package source for this repository.
# This pin keeps machine-level registry mirrors (corporate feeds, proxies)
# out of pnpm-lock.yaml; audit-repository.sh rejects lockfiles that resolve
# tarballs anywhere else.
registry=https://registry.npmjs.org/
